{
  "name": "Training and Mentoring Code Review Validation",
  "passThreshold": 0.85,
  "scenarios": [
    {
      "id": "tm-review-1",
      "title": "Code review as teaching moment",
      "code": "// Junior developer's first PR\nfunction getUserData(id) {\n  var user = db.query('SELECT * FROM users WHERE id = ' + id);\n  if (user == null) {\n    return -1;\n  }\n  return user;\n}",
      "expectedFindings": ["SQL injection vulnerability -- teach parameterized queries", "Returning -1 as error code -- teach exception handling", "Using var instead of const/let -- teach modern conventions", "SELECT * -- teach explicit column selection", "Loose equality (==) -- teach strict equality", "NOTE: Frame all findings as learning opportunities, not criticisms"],
      "severity": "warning"
    }
  ]
}
