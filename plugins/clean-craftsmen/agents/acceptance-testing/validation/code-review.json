{
  "name": "Acceptance Testing Code Review Validation",
  "passThreshold": 0.85,
  "scenarios": [
    {
      "id": "at-review-1",
      "title": "Acceptance test written in code language",
      "code": "describe('POST /api/orders', () => {\n  it('should insert a row into the orders table with status pending and return 201 with the order_id in the JSON body', () => {\n    const response = post('/api/orders', { items: [{ sku: 'ABC', qty: 2 }] });\n    expect(response.status).toBe(201);\n    expect(db.query('SELECT * FROM orders WHERE id = ?', [response.body.order_id])).toBeTruthy();\n  });\n});",
      "expectedFindings": ["Test describes implementation (SQL table, HTTP status code) not business behavior", "Should be in Given-When-Then business language", "Tests database internals -- acceptance tests should test at the boundary", "Name describes technical details, not business scenario"],
      "severity": "warning"
    }
  ]
}
