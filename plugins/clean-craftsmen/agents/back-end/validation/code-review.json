{
  "name": "Back-end Code Review Validation",
  "passThreshold": 0.85,
  "scenarios": [
    {
      "id": "be-review-1",
      "title": "Business logic in controller",
      "code": "class OrderController:\n    def create_order(self, request):\n        items = request.json['items']\n        total = sum(i['price'] * i['qty'] for i in items)\n        if total > 1000:\n            total *= 0.95  # 5% discount\n        if request.user.is_premium:\n            total *= 0.9  # premium discount\n        order = db.execute('INSERT INTO orders (user_id, total) VALUES (?, ?)',\n                          [request.user.id, total])\n        send_email(request.user.email, f'Order {order.id} confirmed')\n        return {'order_id': order.id, 'total': total}",
      "expectedFindings": ["Business logic (pricing, discounts) belongs in domain layer, not controller", "Raw SQL in controller -- use repository pattern", "Email sending should be async/separate concern", "No input validation", "No error handling"],
      "severity": "warning"
    }
  ]
}
