{
  "name": "Machine Language Kata",
  "passThreshold": 0.8,
  "katas": [
    {
      "id": "ml-hw-kata-1",
      "title": "Explain What the Compiler Does",
      "prompt": "Given this high-level function, explain what happens at the machine level: function calls, stack frames, register usage, and memory access patterns.\n\nfunction sum(numbers) {\n  let total = 0;\n  for (let i = 0; i < numbers.length; i++) {\n    total += numbers[i];\n  }\n  return total;\n}",
      "criteria": ["Stack frame created for function", "Loop counter likely in register", "Array access pattern described (sequential, cache-friendly)", "Accumulator in register", "Return value convention explained", "Connection to why this is efficient (sequential memory access)"]
    }
  ]
}
