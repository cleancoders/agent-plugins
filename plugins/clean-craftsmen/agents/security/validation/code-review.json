{
  "name": "Security Code Review Validation",
  "passThreshold": 0.85,
  "scenarios": [
    {
      "id": "sec-review-1",
      "title": "Hardcoded credentials",
      "code": "DB_PASSWORD = 'super_secret_123'\nconnection = connect(host='prod-db.internal', user='admin', password=DB_PASSWORD)",
      "expectedFindings": ["Hardcoded credentials in source code", "Secrets must be externalized (env vars, vault, config service)", "Using admin account -- violates least privilege"],
      "severity": "critical"
    }
  ]
}
