{
  "name": "Package Principles Code Review Validation",
  "passThreshold": 0.85,
  "scenarios": [
    {
      "id": "pkg-review-1",
      "title": "Circular package dependency",
      "code": "// Package A\nimport { UserValidator } from 'package-b';\nexport class UserService { validate(u) { return new UserValidator().check(u); } }\n\n// Package B\nimport { UserService } from 'package-a';\nexport class UserValidator { check(u) { return new UserService().exists(u.email); } }",
      "expectedFindings": ["Circular dependency between Package A and Package B (ADP violation)", "Break cycle by extracting an interface or creating a third package", "Cannot build or release either package independently"],
      "severity": "critical"
    }
  ]
}
