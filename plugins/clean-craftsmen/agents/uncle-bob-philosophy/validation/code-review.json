{
  "name": "Uncle Bob Philosophy Code Review Validation",
  "passThreshold": 0.85,
  "scenarios": [
    {
      "id": "ub-review-1",
      "title": "Unprofessional commit",
      "code": "// Commit message: 'quick fix, no time to test, will clean up later'\n// Changes: Modified authentication logic, removed input validation\n// for speed, added TODO comments for 'later'\n\nfunction authenticate(user, pass) {\n  // TODO: add validation back later\n  // TODO: add tests\n  // TODO: handle edge cases\n  return db.query(`SELECT * FROM users WHERE name='${user}' AND pass='${pass}'`);\n}",
      "expectedFindings": ["Oath violation: knowingly producing defective code", "Removed input validation (harmful to users)", "SQL injection vulnerability (harmful code)", "No tests (violates oath item 3)", "'Will clean up later' is a professional lie -- later never comes", "This commit should not have been made"],
      "severity": "critical"
    }
  ]
}
